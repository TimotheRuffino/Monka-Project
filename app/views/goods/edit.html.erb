<h1>Editing Good</h1>
<!-- Goods Form Fields -->
<%= form_with(model: @goods, local: true, method: :put) do |field| %>
  <div class="form-group">
    <div class="form-row">
      <div class="col">
        <%= label_tag 'Produit/service :' %>
        <%= field.text_field :title, class:"form-control", placeholder:"produit/service" %>
      </div>
      <div class="col">
        <%= label_tag 'Description :' %>
        <%= field.text_field :description, class:"form-control", placeholder:"" %>
      </div>
    </div>
  </div>

  <div class="form-group">
    <div class="form-row">
      <div class="col-7">
        <%= label_tag 'Quantité :' %>
        <%= field.number_field :quantity, class:"form-control", placeholder:"" %>
      </div>
      <div class="col-5">
        <%= label_tag 'prix :' %>
        <%= field.number_field :price, class:"form-control", placeholder:"" %>
      </div>
    </div>
  </div>


    <div class="form-group my-5">
      <div class="row justify-content-center">
        <%= submit_tag "Valider", {class: 'btn btn-primary'} %>
      </div>
    </div>
  </div>
<% end %>


<%= render 'form', good: @good %>

<%= link_to 'Show', @good %> |
<%= link_to 'Back', goods_path %>
